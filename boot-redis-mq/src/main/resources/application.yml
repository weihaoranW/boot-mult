## Tomcat\u914D\u7F6E
server:
  port: 9999
  tomcat:
    uri-encoding: UTF-8

spring:
  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    virtual-host: live-mall
    username: guest
    password: guest
    publisher-confirms: true

logging:
  level:
    root: DEBUG
  path: logs
  #file: logs.log